# pomodorosh

Pomodoro bash script.

Runs as a background process, supports Luxafor lights, writes remaining time in colorcoded XML for use with command monitors.

Uses [luxafor-python](https://github.com/vmitchell85/luxafor-python) for Luxafor support.

requires `ogg123` (`vorbis-tools` on debian) for audio playback
